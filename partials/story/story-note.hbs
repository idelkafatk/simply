{{!-- Note row (microblog / forum-thread style). Used in the /notes/ timeline. --}}
<article class="story-note js-story block py-8 border-b border-gray-300" data-note-url="{{url}}">
    <div class="flex flex-col min-w-0">
        <a href="{{url}}" class="story-note-main story-note-permalink block no-underline">
            <div class="story-note-meta flex items-center mb-3 text-sm text-gray-500">
                <time class="capitalize" datetime="{{date format="YYYY-MM-DD"}}">{{date format="D MMMM YYYY"}}</time>
            </div>

            <h2 class="story-note-title text-xl font-semibold leading-tight text-title mb-3">{{title}}</h2>
        </a>

        <div class="story-note-body text-lg leading-normal text-global">{{content words="200"}}</div>

        {{#if feature_image}}
        <a href="{{url}}" class="story-note-permalink block no-underline" aria-label="{{title}}">
            <figure class="story-note-media mt-4 overflow-hidden rounded-lg border border-gray-300">
                <img class="block w-full max-h-96 object-cover blur-up lazyload"
                    src="data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw=="
                    data-srcset="{{img_url feature_image size="s"}} 300w, {{img_url feature_image size="m"}} 600w, {{img_url feature_image size="l"}} 1000w"
                    data-sizes="auto"
                    alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}" />
            </figure>
        </a>
        {{/if}}

        {{#has tag="count:>1"}}
            <div class="story-note-footer mt-4 text-sm text-gray-500">
                {{#foreach tags}}
                    {{#match visibility "=" "public"}}
                        <a class="note-topic-link" href="/notes/{{slug}}/">#{{name}}</a>
                    {{/match}}
                {{/foreach}}
            </div>
        {{/has}}

        {{!-- Keep the full post source inert; JavaScript extracts Ghost Gallery cards. --}}
        <div class="story-note-gallery" data-note-gallery></div>
        <template data-note-gallery-source>{{content}}</template>
    </div>
</article>
